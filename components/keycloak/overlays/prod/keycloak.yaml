apiVersion: k8s.keycloak.org/v2alpha1
kind: Keycloak
metadata:
  name: keycloak-prod
  namespace: keycloak-system
spec:
  instances: 1
  unsupported:
    podTemplate:
      spec:
        containers:
          - name: keycloak
            args: ["start-dev"]
